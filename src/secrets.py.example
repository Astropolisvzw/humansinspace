# WiFi Configuration Template
# Copy this file to secrets.py and add your WiFi credentials

# List of WiFi networks (will try in order)
WIFI_NETWORKS = [
    {"ssid": "your_home_wifi", "password": "your_password"},
    # Add museum network here when you get the credentials:
    # {"ssid": "museum_wifi", "password": "museum_password"},
]

# Backward compatibility
SSID = WIFI_NETWORKS[0]["ssid"]
PASSWORD = WIFI_NETWORKS[0]["password"]
